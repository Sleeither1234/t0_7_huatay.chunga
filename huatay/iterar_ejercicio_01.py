#impresion de cada valor de una cadena ingresada
import os
nombre=(os.sys.argv[1]) #asignacion de la cadena FERNANDO a la variable nombre
for c in nombre:#repeticion de las intrucciones hasta que la variable nombre se agote
    print(c) #se imprime el valor de la variable c
    #fin_iterar
print("fin del bucle") #se imprime el comentario del fin del programa

